{
  "name": "goat-domino-score",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "check": "node --check game.js && node --check main.js",
    "test": "node --test"
  }
}
